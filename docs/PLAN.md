# Plan de implementación — Sitio PARTA

Documento vivo. Cada vez que analicemos un componente nuevo se agrega su sección acá, con notas suficientes para retomar sin tener que re-derivar contexto.

## Estado de ramas (referencia)

- `main` — sitio "anterior": `VideoZoom` (hero video scroll-driven) + `IsometricMap` + placeholder. Es lo que está deployado/estable hoy.
- `sitio-provisorio` — landing temporal para publicar mientras se construye el sitio completo: solo `Hero` (slider de imágenes) + `Footer`.
- `develop` — sitio completo en construcción. Orden actual de `app/page.tsx`: `Hero` → sección "Características del proyecto" (placeholder, sin diseño todavía) → `VideoZoom` → `IsometricMap` → `Footer`. Acá es donde se va a integrar cada componente nuevo del plan.

## Componentes

### 1. Mapa interactivo de loteo (plano cenital) — DESKTOP Y MOBILE IMPLEMENTADOS

Componente distinto de `IsometricMap.tsx` (ese es una vista pseudo-3D dibujada en SVG, agrupa por zona completa, ya existe y funciona). Este es un **plano cenital sobre imagen real del predio**, con interacción a nivel de **lote individual**, no de zona. Pensado para un paso más avanzado del funnel (el usuario ya eligió una zona/rubro y quiere ver o reservar un lote puntual).

#### Estado actual

- **Componentes**: `app/components/CenitalMap.tsx` es un wrapper que monta `CenitalMapDesktop.tsx` (`hidden md:block`) o `CenitalMapMobile.tsx` (`md:hidden`, breakpoint `md`=768px como el resto del sitio). Integrado en `develop` → `page.tsx` antes de `IsometricMap` (orden pedido por el cliente, ver sección "Dónde va" más abajo). La card de detalle de lote (`LOTE {n}`, m², pills, Consultar) vive en `LoteDetailCard.tsx`, compartida por ambas variantes. El cálculo de escala/offset de un `<svg viewBox>` (cover fit) para posicionar overlays HTML está en el hook `useMapScale.ts`, también compartido.
- **Dataset**: `app/data/lotes.ts` — 64 lotes trazados a mano por diseño en `designs/mapa-cenital/desktop/mapa-loteo-editable.svg` (ver abajo), agrupados en 6 zonas por **rubro** (no por posición — ver nota de zonas mobile más abajo). Reemplaza el modelo `Bloque`/subdivisión sintética que se había armado en un primer intento (ver "Pasos" más abajo).
- **Fondo real desktop**: `public/images/mapa/mapa-base.png` (1920×1080) — **no** es `designs/mapa-cenital/desktop/mapa-base.jpg` (1680×936, quedó obsoleto para este componente). Ver nota de coordenadas.
- **Geometría real preservada**: cada lote es un polígono SVG con su forma real (esquinas redondeadas y bordes diagonales incluidos), no un rectángulo aproximado — se sampleó cada curva bezier del SVG original en vez de reducir todo a bounding box.
- **Interacción**: click en un lote abre una card (mismo lenguaje chamfer del resto del sitio) con número, m², pill de rubro, pill de estado y "Consultar" (antes decía "Reservar", cambiado a pedido del cliente) → scrollea a `#contacto`. Polígonos con fondo/borde blanco transparente (se iluminan en hover), el color de zona vive solo en el marcador (punto) de cada lote — así calca el mockup, donde el color únicamente aparece en los puntitos. La card se posiciona con su "tallo" siempre apuntando al marcador real, pero se desplaza horizontalmente si hace falta para no cortarse contra el borde de pantalla (bug encontrado probando en mobile, donde el margen es mucho más chico que en desktop — ver `LoteDetailCard.tsx`).
- **⚠️ Nota clave sobre coordenadas desktop** (para no repetir el error): `designs/mapa-cenital/desktop/mapa-loteo-editable.svg` tiene una capa oculta con la imagen de referencia y `transform="scale(1.2)"` — **ese factor NO se aplica** a la posición final de los lotes. Lo que sí aplica es el `clipPath` (offset 11.4, 63.1 sobre un recorte de 1920×1080): ese recorte, exportado tal cual, es `designs/mapa-cenital/desktop/mapa-base-ok.png` (ahora `public/images/mapa/mapa-base.png`), y los lotes de `Layer_2` viven en el mismo sistema de coordenadas que ese recorte. Es decir: **restar el offset del clip, sin escalar nada**. Costó varias vueltas encontrar esto — si se vuelve a tocar el trazado, empezar por acá.
- **❌ Probado y descartado — título "Descubrí todo lo que PARTA tiene para ofrecer."** (2026-07-29): viene de `designs/mapa-cenital/mobile/mapa-mobile-1.webp` (mockup mobile original; el cliente había pedido sacarlo en una vuelta anterior). Se probó agregarlo de nuevo como overlay absoluto en el área vacía de campo/cielo arriba del predio, en ambas variantes (mobile solo en `ListaZonas`, desktop siempre visible) — no convenció visualmente y se sacó otra vez. Si se vuelve a pedir, repensar el enfoque (no como overlay `position: absolute` suelto sobre la imagen) en vez de retomar la implementación anterior tal cual.
- **Pendiente**: posición final en `page.tsx`, setear `SHEET_JSON_URL` en el hosting al deployar. (Rubros, numeración por lote, m² y estado vía Google Sheet ya están confirmados/conectados, ver abajo.)

#### Variante mobile — implementada

Primera versión (2026-07-27) tenía un bug: al llegar a la sección en mobile, el mapa desktop (1920×1080, muy horizontal) se mostraba con `preserveAspectRatio="xMidYMid slice"` dentro de un contenedor angosto y vertical, así que el "cover fit" recortaba los bordes izquierdo/derecho de la imagen — la Zona 1 (esquina superior izquierda) quedaba fuera de cuadro. El cliente generó **dos assets nuevos específicos para mobile** (mismo predio real, recorte/composición distinta, pensada para pantalla vertical):

| Archivo | Uso | Dimensiones |
|---|---|---|
| `designs/mapa-cenital/mobile/mapa-loteo-base-mobile.png` (copiado a `public/images/mapa/mapa-base-mobile.png`) | Fondo real para mobile — mismo predio, exportado como recorte vertical completo pensado para pantalla, con bastante cielo/campo vacío arriba y abajo del predio (el componente lo recorta en código, ver más abajo). | 2160×3840 |
| `designs/mapa-cenital/mobile/mapa-loteo-editable-mobile.svg` | Trazado de los 64 lotes sobre ese fondo (mismo patrón que el SVG desktop: capa oculta de referencia + `clipPath`). | viewBox 1257.8×2427, `clipPath` en `x=80.6 y=191 w=1080 h=1920` |

**⚠️ Nota de coordenadas mobile** (mismo patrón que desktop, pero con un factor de escala extra): el `clipPath` define el recorte final (1080×1920), y el PNG final (2160×3840) es exactamente ese recorte **exportado al doble de resolución** (2160/1080 = 3840/1920 = 2). La transformación de un punto del SVG a píxel real del PNG es: `((x − 80.6) × 2, (y − 191) × 2)` — sin rotación. Verificado dibujando los 64 polígonos transformados como overlay sobre el PNG real: coinciden pixel-perfect con los bloques de la foto.

**⚠️⚠️ El SVG mobile no numera los lotes, y el orden del documento NO es el número de lote** — este fue el hallazgo más importante y el que más se puede repetir si se vuelve a tocar: las 64 formas de `Layer_1` no tienen ningún atributo de número/id, y probar "shape N del documento = lote N" (lo que se asumió al principio) da un resultado **incorrecto** — el shape #37 del documento es en realidad el Lote 1. La correspondencia forma→número se resolvió por **matching geométrico**: se tomaron los centroides ya confirmados de los 64 lotes en `app/data/lotes.ts` (dataset desktop, coordenadas 1920×1080) y los centroides de las 64 formas mobile (coordenadas 2160×3840, ya transformadas), se ajustó una transformación de similitud (escala X/Y independiente + traslación, sin rotación) por bounding box, y se hizo un matching de vecino más cercano iterado 4 veces (recalculando la transformación con los pares ya emparejados en cada vuelta). Convergió con error promedio 18.8px y máximo 51px sobre lotes de ~60-90px — sin ambigüedad. Se verificó además que: (a) es una biyección exacta (los 64 números 1-64 aparecen una sola vez), y (b) visualmente los rubros forman clusters coherentes con el dataset desktop (todos los lotes de un mismo rubro quedan agrupados). Si se vuelve a pedir un asset SVG nuevo (otra zona, versión 2, etc.), **pedir que el cliente incluya el número de lote como atributo o label en cada shape** para no tener que repetir este proceso.

- **Dataset**: `app/data/lotesMobileGeometry.ts` — exporta `MAPA_MOBILE_WIDTH`/`MAPA_MOBILE_HEIGHT` (2160×3840), `LOTE_GEOMETRY_MOBILE: Record<numero, {polygon, centro}>` (generado por el proceso de arriba) y `MAPA_MOBILE_PARK_BBOX` (bbox real de los 64 lotes, con margen — usado para el nivel 1). Los demás campos del lote (m², estado, rubro, color) siguen viniendo de `app/data/lotes.ts` / el Sheet — `CenitalMapMobile` combina ambos por `numero` en el momento de renderizar, no hay un dataset "Lote mobile" duplicado.
- **Zonas de navegación mobile**: `app/data/zonasMobile.ts` — **2 zonas posicionales** (mitad izquierda / mitad derecha del predio), no confirmadas por el cliente en tanto agrupación de UX, pero sí la división exacta lote-por-lote (`designs/mapa-cenital/mobile/loteo-zona-1.jpg` / `loteo-zona-2.jpg`, pasados por el cliente). Se probó primero con 6 zonas (una por manzana real), pero el tamaño físico de cada manzana variaba demasiado (de 3 lotes enormes a 15 lotes en una tira angosta) y el zoom quedaba con una escala muy distinta según la zona — con el ancho de pantalla como límite real en mobile (no el alto, que sobra), 2 zonas resuelve eso sin perder nada partiendo a la mitad.
- **Flujo**: nivel 1 (`ListaZonas`) — sección `h-screen` con el mapa como fondo a pantalla completa (cover-fit) y la lista de zonas superpuesta abajo (`absolute bottom-8`); tocar una zona resalta sus lotes y muestra un tag flotante sobre el bloque en el mapa, con botón "Ingresar". Nivel 2 (`ZonaZoom`) — mismo `<svg viewBox>` y mismo contenedor `h-screen` que el nivel 1 (nunca cambian), lotes clickeables con marcador de color, flecha atrás vuelve al nivel 1. Misma `LoteDetailCard` que desktop en ambos niveles.
- **⚠️ Historia del recorte en pantalla — 3 iteraciones hasta llegar a esto, para no repetirlas**:
  1. Mostrar el PNG completo (2160×3840) sin recortar: correcto en el sentido de "no perder nada", pero con tanto cielo/campo vacío alrededor del predio que quedaba un scroll enorme en negro antes de ver algo.
  2. Recortar el `viewBox` al bbox real de los lotes (con margen) **por nivel** — un bbox para la lista completa y OTRO bbox distinto (más angosto) por zona al hacer zoom, cada uno con el contenedor ajustado a `style={{ aspectRatio: w/h }}` para que el cover-fit no recortara de más. Esto resolvía el recorte pero generaba un bug distinto y más notorio: como el bbox de cada nivel tiene una proporción ancho:alto diferente (el bbox del parque completo es bien horizontal; el de una zona, con el padding vertical agregado, es más vertical), el **contenedor cambiaba de alto entre nivel 1 y nivel 2** — en mobile eso se veía como el mapa general aplastado en una tira horizontal corta arriba de la pantalla (con un bloque negro enorme debajo para los botones), y al hacer zoom "se agrandaba todo el fondo" en vez de sentirse como un acercamiento horizontal prolijo.
  3. **Solución actual**: un único `viewBox` base (`MAPA_MOBILE_PARK_BBOX`, con padding fuerte en Y y modesto en X para lograr una proporción retrato razonable — ver `PARK_PAD_X`/`PARK_PAD_TOP`/`PARK_PAD_BOTTOM` en `lotesMobileGeometry.ts`) usado por **los dos niveles por igual**, en un contenedor `h-screen` que nunca cambia de tamaño. El "zoom" a una zona no es un recorte de `viewBox` nuevo: es una transformación `transform="translate(ox,oy) scale(s) translate(-ox,-oy)"` aplicada a un `<g>` con la imagen + los lotes (ver `ZONA_MOBILE_ZOOM` en `zonasMobile.ts`), anclada al borde izquierdo o derecho del bbox base según de qué lado está la zona (ese punto queda fijo en pantalla, el resto "crece" hacia el lado opuesto) y al centro vertical del bbox base. Esto replica el comportamiento de los assets de referencia en `designs/mapa-cenital/mobile/` (`mapa-general.png` vs `mapa-zona-1.jpg`/`mapa-zona-2.jpg`: mismo encuadre vertical entre las tres, el zoom es puramente horizontal) y es el enfoque que ya estaba anotado como "plan original, todavía no implementado" más abajo en este documento — terminó siendo la solución correcta. Los overlays HTML (`LoteDetailCard`) aplican la misma transformación a mano sobre el punto antes de convertirlo a píxeles de pantalla (`applyZoom` en `CenitalMapMobile.tsx`), ya que no son hijos del `<svg>` y no heredan el `transform` del `<g>`.
  - Nota aparte, ortogonal a lo anterior: en los tres intentos hubo un bug de recorte de la **card de detalle y el tag flotante de zona** contra el borde superior de su contenedor — la causa era que ambos son overlays HTML posicionados con `-translate-y-full` (cuelgan hacia arriba de su ancla) dentro de un div con `overflow-hidden` puesto ahí solo para el cover-fit de la imagen (innecesario: el propio `<svg>` ya clippea su contenido por default). Sacar el `overflow-hidden` del div contenedor (dejando el `<svg>` con su propio clip) resuelve el recorte sin tocar el padding.
- **✅ Resuelto — lote pegado al borde de pantalla al hacer zoom a una zona** (reportado 2026-08-06): la fórmula de `scale` en `ZONA_MOBILE_ZOOM` (`zonasMobile.ts`) hacía que el borde de la zona más cercano al centro del predio llegara **exacto** al borde opuesto de la pantalla (0px de aire) — a propósito, per la nota de arriba ("la zona pasa a ocupar la pantalla completa"), pero en dispositivo real el lote pegado a ese borde quedaba visualmente muy ajustado. El borde exterior (anclado en `originX`) no tenía este problema — al escalar, ese lado *gana* aire en vez de perderlo (crece alejándose del punto fijo). Fix: se resta un margen (`ZOOM_INNER_MARGIN = 70`, mismo orden de magnitud que `PARK_PAD_X`) del ancho objetivo antes de calcular la escala, así el zoom no llega al 100% del ancho y ambos bordes quedan con aire. No toca el recorte vertical (ese lo decide el `viewBox`/aspect ratio del `<svg>` exterior, independiente de este transform).
- **✅ Resuelto — Bug del lote 13 en mobile** (reportado 2026-07-28, diagnosticado y arreglado 2026-07-29): en Zona 02, el lote 13 no se resaltaba igual que el resto al seleccionar la zona y no abría `LoteDetailCard` al clickearlo ampliado (funcionaba bien en desktop). Causa raíz: el polígono de `LOTE_GEOMETRY_MOBILE[13]` (el lote con el corte diagonal, el más irregular del set) estaba **mal formado** — en vez de un triángulo sólido, el trazado extraído del SVG recorría el contorno "de ida y de vuelta" muy cerca de sí mismo (artefacto de la curva bezier con esquinas redondeadas + borde diagonal), dejando el relleno real reducido a un marco delgado en dos bordes — confirmado con `SVGGeometryElement.isPointInFill()` en vivo, no alcanzaba con un point-in-polygon a mano. Su propio centroide (`centro: [1338.5, 1523]`, calculado igual que el resto por promedio/estimación) caía **fuera** de ese relleno real: el marcador y la card apuntaban a un punto fuera del lote, y un tap ahí cliqueaba la imagen de fondo en vez del polígono. Desktop no tenía el bug porque usa un dataset distinto para ese lote (un triángulo simple con centroide calculado dinámicamente por `centroide()` en `app/data/lotes.ts`). Fix: se reconstruyó el polígono como la envolvente convexa de los mismos puntos originales (son válidos individualmente, solo el orden/trazado estaba roto), con centroide recalculado y verificado adentro — ver el comentario en `lotesMobileGeometry.ts` línea ~27. Vale la pena tenerlo presente si se vuelve a re-trazar el SVG mobile: **verificar con `isPointInFill` (no solo point-in-polygon) que el centroide de cada lote cae dentro de su propio relleno**, en especial en las formas no rectangulares.
- **✅ Resuelto — bloque "volver + nombre de zona" reubicado** (nivel 2, `ZonaZoom`): estaba `absolute top-4 left-4`, superpuesto arriba del mapa — la navegación quedaba incómoda (click abajo para "Ampliar", arriba para "Volver") y la card de detalle de un lote (que cuelga hacia arriba) lo tapaba justo cuando más se necesitaba. Ahora `absolute bottom-24 left-6`: misma altura y margen izquierdo que los botones de zona del nivel 1 (`bottom-24`, contenedor con `px-6`), así toda la navegación queda a mano en la misma zona de la pantalla entre los dos niveles.
- **✅ Resuelto — diseño del botón volver/nombre de zona aprobado** (2026-07-31): flecha + chip con el nombre de zona, mismo lenguaje chamfer del resto del sitio, en `bottom-24 left-6` (ver `CenitalMapMobile.tsx` líneas ~248-265). Sin cambios pendientes.
- Nada más bloqueante — próximo ajuste fino sería decidir si conviene animar la transición de zoom entre nivel 1 y 2 (hoy es un cambio de `transform` sin transición CSS) si se nota muy abrupto en dispositivo real.

#### Assets de referencia (`designs/mapa-cenital/`)

Reorganizados el 2026-08-06 (antes vivían sueltos en `designs/` y después en `designs/trash/` — "trash" era engañoso, no es basura, son los insumos de diseño/fuente de datos de este componente). Quedaron en tres subcarpetas: `desktop/`, `mobile/` (assets activamente citados más abajo) y `historial/` (bocetos/iteraciones superadas y screenshots de bugs ya resueltos — se conservan como referencia pero ninguno es fuente de nada vigente).

| Archivo | Uso | Dimensiones |
|---|---|---|
| `desktop/mapa-base.jpg` | Fondo, sin overlay. Render aéreo nocturno estilizado (no es captura cruda de Google Maps), exportado desde Adobe Illustrator. | 1680×936 |
| `desktop/mapa-interactivo.webp` | Mockup de referencia del overlay interactivo en desktop, con un popup de ejemplo abierto (Lote 14). No es un asset a usar tal cual, es guía visual. | 3841×2161 |
| `mobile/mapa-mobile-1.webp` | Mobile, estado inicial: mapa completo + lista vertical de zonas (ZONA 01–06) sin selección. | — |
| `mobile/mapa-mobile-2.webp` | Mobile, con "ZONA 01" seleccionada en la lista: la entrada pasa a fondo blanco + botón "INGRESAR", y aparece un tag flotante sobre el bloque correspondiente en el mapa. | — |
| `mobile/mapa-mobile-3.webp` | Mismo patrón con "ZONA 04" seleccionada — confirma que el tag flotante se reposiciona según la zona. | — |
| `mobile/mapa-mobile-3-seleccionado.webp` | Mobile, segundo nivel: tras tocar "Ingresar", el mapa hace zoom/crop a esa manzana puntual mostrando lotes individuales como cuadrados de color. Flecha "atrás" (←) arriba-izquierda para volver. Al tocar un lote se abre la card de detalle (Lote 29, 5.066 m², Industrias Alimentarias, Disponible, Reservar). | — |
| `desktop/mapa-loteo-editable.svg` | **Fuente real de los polígonos** (trazado sobre `mapa-base-ok.png`, capa oculta de referencia + `Layer_2` con los 64 lotes). Ver nota de coordenadas arriba. | viewBox 2088.9×1163.8 |
| `desktop/mapa-base-ok.png` | Export final ya recortado (clip-path) — es el que se usa como fondo real del componente (copiado a `public/images/mapa/mapa-base.png`). | 1920×1080 |
| `desktop/muestra-mapa-loteo.png` | Referencia B/N de la forma exacta de cada lote (esquinas redondeadas, 2 lotes con borde diagonal) — usado para corregir la geometría extraída del SVG. | — |
| `desktop/loteo-numero-orden.jpeg` | Mapa cenital con los 64 lotes numerados y coloreados por rubro — **fuente de la numeración real** (qué número de lote cae en qué posición física). | — |
| `desktop/loteo-detalle-completo.jpeg` | Listado por rubro (nombre + color + lotes incluidos + m² real de cada uno) tal como se pasaría a Google Sheet — **fuente de los 6 rubros confirmados y sus m² reales** (todavía no cargados, ver Fuente de datos abajo). |
| `mobile/mapa-loteo-base-mobile.png` | Fondo real para el mapa mobile (recorte vertical completo del mismo predio, copiado a `public/images/mapa/mapa-base-mobile.png`). | 2160×3840 |
| `mobile/mapa-loteo-editable-mobile.svg` | Trazado de los 64 lotes sobre el fondo mobile — sin numeración, ver nota de coordenadas/matching en la sección de variante mobile. | viewBox 1257.8×2427 |
| `mobile/loteo-zona-1.jpg` / `mobile/loteo-zona-2.jpg` | División real (pasada por el cliente) de los 64 lotes en las 2 zonas mobile — **fuente de qué número de lote va en cada mitad**, ver `app/data/zonasMobile.ts`. | — |
| `mobile/mapa-general.png` | Referencia del encuadre mobile nivel 1 (lista de zonas): recorte vertical del predio con margen generoso arriba/abajo. **Las tres imágenes comparten exactamente la misma proporción 9:16** (`mapa-general.png` es 1638×2912, las de zona 1080×1920 — mismo factor de escala en ambos ejes), confirmado por eso que el "zoom" entre niveles no cambia el encuadre vertical, solo el horizontal — ver nota de recorte en pantalla arriba. | 1638×2912 |
| `mobile/mapa-zona-1.jpg` / `mobile/mapa-zona-2.jpg` | Referencia del encuadre mobile nivel 2 (zoom a zona): mismo encuadre vertical que `mapa-general.png`, recortado/ampliado horizontalmente con el borde real del predio (izquierdo en zona 1, derecho en zona 2) flush contra el borde de la imagen. | 1080×1920 |
| `historial/error-1.png` | Screenshot del bug reportado 2026-07-28 (mapa pegado arriba, tag de zona cortado, espacio negro grande debajo de los botones) — ya resuelto, ver iteración 3 de la nota de recorte arriba. Se deja como referencia histórica del bug, no del resultado esperado. | — |
| `historial/` (resto) | Iteraciones previas del trazado SVG y screenshots de otros bugs ya resueltos (`mapa-editable.svg`, `mapa-general.svg`, `mapa-loteo.svg`, `mapa-loteo-editable-1.png`, `mapa-zona-1.svg`, `mapa-zona-2.svg`, `screenshot-mapa.png`, `error-2.png`, `error-3.png`, `estado-lotes-aplicacion.png`, `estado-lotes-original.png`, `estado-posicion-inicial.png`) — ninguno es fuente de datos vigente, quedan solo como historial si hace falta reconstruir el razonamiento de alguna decisión pasada. | — |

#### Flujo de interacción — Desktop

Según `mapa-interactivo.webp`: todos los lotes del predio son visibles y clickeables directamente sobre el plano completo (sin paso intermedio de "elegir zona" como en mobile). Al hacer click en un lote:

- Aparece una card flotante conectada al marcador por una línea ("pin callout"), con esquinas cortadas (chamfer) — mismo lenguaje visual que `ChamferOutline.tsx` / `chamfer.ts`, **reusable directo**.
- Contenido de la card: `LOTE {n}` + ícono, superficie en m², pill de rubro (texto/color según rubro), pill de estado ("DISPONIBLE" en verde — implica que existen más estados: reservado/vendido, con sus propios colores), botón "RESERVAR →", botón cerrar (×).

#### Flujo de interacción — Mobile (2 niveles de drill-down)

1. **Vista completa**: lista vertical de zonas debajo del mapa (ZONA 01 a 06). Tocar una zona → la entrada de la lista se resalta (fondo blanco + botón "Ingresar") y aparece un tag flotante con el nombre de la zona sobre el bloque correspondiente en el mapa (con colita apuntando al bloque), que se resalta mientras el resto se oscurece.
2. **Vista de zona**: al tocar "Ingresar", el mapa hace zoom/crop a esa manzana, mostrando los lotes individuales como marcadores de color. Flecha atrás para volver al nivel 1. Tocar un lote abre la misma card de detalle que en desktop.

**Por qué mobile tiene un paso extra que desktop no**: en pantalla chica no entran todos los lotes clickeables a la vez con buen tamaño de touch-target: se resuelve el zoom en dos pasos (parque → zona → lote) en vez de mostrar todo de una.

#### Insight de datos: color = rubro

Confirmado con `mapa-mobile-3-seleccionado.webp`: los lotes rojos son "Industrias Alimentarias" y el pill de rubro en la card usa ese mismo rojo. El color del marcador **codifica el rubro/industria**, no el estado de disponibilidad (el estado tiene su propio pill, ej. verde = disponible). Esto valida reusar la misma taxonomía de rubros que ya existe en `ZONES` dentro de `app/components/IsometricMap.tsx`.

**✅ Resuelto** (antes pendiente confirmar 4/6 rubros): el cliente pasó `loteo-numero-orden.jpeg` (numeración real por posición) + `loteo-detalle-completo.jpeg` (rubro/color/m² por lote). Los 6 rubros y su color están confirmados en `app/data/lotes.ts` (`confirmada: true` en las 6 entradas de `ZONAS`):

| Rubro | Color | Lotes |
|---|---|---|
| Instalaciones del Parque | Azul `#3b82f6` | 1, 13, 37, 38 |
| Logística y Tecnología | Celeste `#38bdf8` | 2–12 |
| Industrias Varias | Blanco `#f8fafc` | 14–26 |
| Industrias Alimentarias | Rojo `#ef4444` | 27–36 |
| Industrias Metalúrgicas | Violeta `#a855f7` | 39–50 |
| Industrias Agrícolas | Verde `#22c55e` | 51–64 |

Nota: el verde de "Industrias Agrícolas" coincide con `ESTADO_COLOR.disponible` (también verde) — no es un bug, pero puede generar ambigüedad visual entre "lote del rubro agrícola" y "lote disponible" en la card de detalle; vale la pena revisarlo con diseño si se nota confuso en producción.

`IsometricMap.tsx` sigue con su propia taxonomía de 5 zonas (agrícola, metalúrgica, alimentaria, tecnología/logística, administración), sin actualizar todavía contra esta lista de 6 — pendiente si se decide unificar.

#### Modelo de datos por lote (implementado, ver `app/data/lotes.ts`)

```ts
interface Lote {
  id: string;           // "lote-29"
  numero: number;        // 29
  m2: number;             // real (M2_REAL), sobreescribible por Google Sheet
  estado: "disponible" | "reservado" | "vendido"; // mock, sobreescribible por Google Sheet
  zonaId: string;
  zonaLabel: string;      // nombre de rubro — confirmado en las 6 zonas (ver arriba)
  color: string;
  polygon: [number, number][]; // px reales sobre mapa-base.png (1920x1080)
  centro: [number, number];    // centroide de área real, no promedio de vértices
}
```

**Fuente de datos — ✅ Google Sheet real conectado y probado en vivo (2026-07-22)**:

- `m2`: ya no es mock. Se transcribió a mano el m² real de cada uno de los 64 lotes desde `loteo-detalle-completo.jpeg` a `M2_REAL` en `app/data/lotes.ts`. El Sheet puede sobreescribirlo si el cliente lo edita (ver abajo).
- `estado` (disponible/reservado/vendido): viene del Sheet real. Sin dato del Sheet, cae a `estadoMock` (determinístico, no random).
- **Pipeline** (`app/lib/sheet.ts` + `mergeLoteOverrides` en `app/data/lotes.ts`): `app/page.tsx` es un Server Component async que llama `fetchLoteOverrides()`, la cual lee `process.env.SHEET_JSON_URL`, hace `fetch(url, { cache: "no-store" })` y espera un array JSON `[{ numero, m2, estado }, ...]`; `mergeLoteOverrides` combina eso sobre el dataset base y el resultado se pasa a `<CenitalMap lotes={...} />` por prop. Sin `SHEET_JSON_URL` configurada, o si el fetch falla, sigue funcionando con el dataset base (m² real + estado mock) — no rompe nada.
- **⚠️ Por qué JSON vía Apps Script y no "Publicar en la web" (CSV/TSV)**: se probó primero con el Sheet publicado como CSV/TSV (`Archivo > Compartir > Publicar en la web`), pero **Google cachea esa copia publicada de su lado** — los cambios en el Sheet tardaban en reflejarse sin que hubiera forma de controlarlo desde nuestro código (ni `cache: "no-store"` lo evita, porque el retraso ocurre antes, en la copia que Google sirve). Se reemplazó por un **Google Apps Script desplegado como Web App** que ejecuta en vivo contra el Sheet en cada request (`SpreadsheetApp.getActiveSpreadsheet()...`) y devuelve JSON — sin esa capa de caché. Script fuente:

  ```js
  function doGet() {
    const sheet = SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];
    const rows = sheet.getDataRange().getValues();
    const header = rows.shift().map((h) => String(h).toLowerCase().trim());

    const numeroIdx = header.indexOf("numero");
    const rubroIdx = header.indexOf("rubro");
    const m2Idx = header.indexOf("m2");
    const estadoIdx = header.indexOf("estado");

    const data = rows
      .filter((r) => r[numeroIdx] !== "" && r[numeroIdx] !== null)
      .map((r) => ({
        numero: Number(r[numeroIdx]),
        rubro: String(r[rubroIdx]),
        m2: Number(r[m2Idx]),
        estado: String(r[estadoIdx]).toLowerCase().trim(),
      }));

    return ContentService.createTextOutput(JSON.stringify(data)).setMimeType(
      ContentService.MimeType.JSON
    );
  }
  ```

  Deploy: Extensiones > Apps Script > pegar este código > Implementar > Nueva implementación > tipo "Aplicación web" > Ejecutar como "Yo" > Acceso "**Cualquier usuario**" (no "Cualquier usuario dentro de \[dominio]" — con el Sheet en un Workspace de Google, esa opción sigue pidiendo login aunque diga "cualquiera"; terminó resuelto armando el Apps Script en una cuenta personal en vez del Workspace de aggility.io).
- `SHEET_JSON_URL` está en `.env.local` (no versionado) apuntando al `/exec` de ese deploy. **✅ Ya seteada también en el hosting** (2026-07-31). `.env.example` documenta la variable.
- **Nota de caché/cuota**: hoy el fetch usa `cache: "no-store"` (se puso así para probar en vivo con el cliente) — cada carga de página ejecuta el Apps Script. Antes de producción, evaluar si conviene un `revalidate` corto (30–60s) para no pegarle a la cuota diaria de ejecuciones de Apps Script en cada visita.

#### Estrategia responsive / coordenadas — resuelto para desktop

Terminó siendo más simple que lo que se había anticipado acá: el cliente pasó el trazado real (`designs/mapa-cenital/desktop/mapa-loteo-editable.svg`, dibujado sobre la imagen de referencia), así que no hizo falta estimar nada a mano. Los polígonos son coordenadas **px absolutas** sobre `mapa-base.png` (1920×1080, no %), y el `<svg viewBox>` del componente + `preserveAspectRatio="xMidYMid slice"` se encarga de escalar todo junto (imagen de fondo y polígonos viven en el mismo `<svg>`, no como capas HTML separadas — así nunca se desalinean entre sí sea cual sea el tamaño de pantalla). Ver la nota de coordenadas más arriba para el detalle de cómo se derivó el offset.

Para el **zoom a nivel zona en mobile**: implementado — ver la "Solución actual" (iteración 3) en la nota de recorte en pantalla de la sección de variante mobile más arriba.

#### Botón "Consultar" (antes "Reservar") — implementado

Hace scroll a `#contacto` (el formulario de `Footer.tsx`). Todavía no pre-carga el número de lote como contexto en el formulario — pendiente si se quiere ese nivel de detalle.

#### Dónde va en `page.tsx` (develop) — resuelto

Orden confirmado por el cliente (2026-08-06): primero `CenitalMap`, después `IsometricMap` (antes estaba al revés, posición tentativa sin confirmar).

#### Pasos sugeridos de implementación

1. ~~Conseguir dataset real de lotes~~ — hecho: geometría real, 6/6 rubros confirmados (nombre, color y numeración real por lote), m² real cargado. Solo `estado` sigue mock, pendiente de que el cliente cargue el Sheet.
2. ~~Conseguir el SVG/AI fuente del plano~~ — hecho, el cliente pasó `mapa-loteo-editable.svg` con el trazado real.
3. ~~Armar el componente en desktop~~ — hecho (`CenitalMap.tsx`).
4. ~~Armar la variante mobile: lista de zonas + tag flotante + transición de zoom a zona + misma card de detalle.~~ — hecho, con fondo/geometría propios para mobile (ver sección arriba).
5. ~~Conectar "Consultar" al formulario~~ — hecho (scroll a `#contacto`; falta pre-cargar el lote como contexto si se quiere).
6. ~~Integrar Google Sheet como fuente de estado de disponibilidad~~ — hecho y probado en vivo con el Sheet real del cliente (vía Apps Script/JSON, ver arriba). `SHEET_JSON_URL` ya seteada tanto en `.env.local` como en el hosting. Falta decidir el `revalidate` final antes de producción (hoy `cache: "no-store"`, ver nota de caché/cuota arriba).
7. Definir posición final en `page.tsx`.

---

### 2. Servicios (3 bloques full-bleed) — IMPLEMENTADO

`app/components/Servicios.tsx`, integrado en `page.tsx` **después de `IsometricMap`, antes de `Footer`** (posición pedida explícitamente por el cliente, no inferida). Assets fuente en `designs/servicios/`: `servicios-{1,2,3}-muestra.png` con el mockup completo (texto + foto, guía visual) y `servicios-{1,2,3}-bg.png` el fondo limpio sin texto (fuente real, convertidos a webp en `public/images/servicios-{1,2,3}-bg.webp`).

- **Tipografía del título**: misma escala que el `h2` de `DescripcionGeneral.tsx` (`font-heading text-3xl md:text-9xl leading-[0.9] md:leading-[0.8] font-bold tracking-tighter uppercase`) — primer intento usó `md:text-8xl`/`leading-[0.85]` por error (una escala más chica), corregido a pedido del cliente (2026-08-07) para que coincida con el tamaño real de esa sección. **La bajada NO terminó igual a `DescripcionGeneral`**: quedó en `text-sm leading-[1.3] md:text-lg` (ajuste manual posterior del cliente en el archivo, más compacta que el `text-base leading-relaxed` de esa sección — a propósito, no un desvío accidental).
- **A diferencia de `descripcion-general-fondo.webp`, estos fondos NO tienen el fade a negro incluido en el alpha** (son PNG opacos) — el degradé que da legibilidad al texto blanco está "horneado" directo en los píxeles de la foto (grading oscuro real, no transparencia), así que el componente no necesita ningún gradient overlay en CSS, solo `object-cover` + texto encima.
- **3 bloques, cada uno con su propio ratio** (`aspect-ratio` igual a las dimensiones nativas del PNG, mismo patrón que el contenedor de foto de `DescripcionGeneral`): bloque 1 y 2 son 1921×1780, bloque 3 (banner de cierre, solo título) es 1921×1117, más bajo. Los 3 títulos van centrados (el mockup del bloque 1 lo mostraba alineado a la izquierda, pero se centró a pedido del cliente para unificar con el resto).
  1. "Alquiler y ventas de naves industriales." — título + bajada + botón CTA centrados, contenido pegado abajo del bloque (`justify-end`).
  2. "Infraestructura, conectividad y seguridad para empresas." — título y bajada centrados, contenido pegado arriba del bloque (`justify-start`).
  3. "64 lotes de hasta 14.000 m²" — solo título, centrado horizontal, **alineado arriba** (`justify-start`, no centrado verticalmente — a diferencia del primer intento).
- **⚠️ Botón "Consultá con un asesor" (bloque 1) — deliberadamente NO usa el lenguaje chamfer del resto del sitio**: en el mockup es una píldora completamente redondeada (`rounded-full`) en azul `#3555fa`, distinto del `--parta-blue` (`#1e4cfa`) usado en todos los demás botones (Hero, "Consultar" de `LoteDetailCard`). Confirmado con el cliente/decisión tomada (2026-08-07): implementado fiel al mockup tal cual, no adaptado al chamfer. Si en algún momento se pide "unificar" los botones del sitio, este es el único que queda afuera del patrón a propósito. Link a `#contacto` (mismo scroll que el resto de los CTA de contacto).
- **Nota de copy**: la bajada del bloque 2 dice "se prevee" (así, con doble e) — es el texto tal cual lo pasó el cliente en el mockup, no un typo introducido acá. Si se quiere corregir a "se prevé", avisar antes de tocarlo (es copy del cliente, no texto inventado).
- **Saltos de línea del título son manuales**, copiados tal cual del mockup de cada bloque (`heading: string[]`, una entrada por línea, unidas con `<br />`) — no queda al criterio del wrap automático del navegador. Bloque 1: 2 líneas ("Alquiler y ventas de" / "naves industriales."). Bloque 2: 4 líneas (una por frase corta: "Infraestructura," / "conectividad y" / "seguridad para" / "empresas."). Bloque 3: 2 líneas ("64 lotes de" / "hasta 14.000 m²" — antes quedaba mal cortado porque se dejaba wrappear solo). Si se agrega un cuarto bloque, seguir el mismo patrón en vez de un string único.
- **Pendiente / sin confirmar**: posicionamiento vertical del texto dentro de cada bloque (paddings `pb-12 md:pb-20` / `pt-16 md:pt-28` / `pt-20 md:pt-24`) es una aproximación visual al mockup, no medido pixel-perfect — recomendable revisar en pantalla real antes de dar por cerrado.
- **✅ Resuelto — texto se salía de la caja arriba/abajo en mobile** (reportado 2026-08-08, cliente sumó `servicios-{1,2,3}-muestra-mobile.png` a `designs/servicios/`): la v1 fijaba `aspect-ratio` a las dimensiones del PNG **desktop** (~1921×1780, bien horizontal) sin importar el viewport — en una pantalla angosta esa caja quedaba muy baja de alto (ej. ~360px en un mobile de 390px de ancho), mucho menos de lo que el stack título+bajada+botón necesita a tamaño mobile, así que el contenido (posicionado con `justify-end`/`justify-start` + padding) se desbordaba por el borde opuesto. Los mockups mobile confirman que la proporción real pensada para esa pantalla es **retrato** (~1080×1921, ~9:16, medido de los propios PNG), muy distinta a la de desktop. Fix: `aspect-[1080/1921] md:aspect-[1921/1780]` (clases Tailwind responsive, reemplazando el `style={{aspectRatio}}` fijo de la v1) — con la caja correctamente alta en mobile, el mismo `justify-end`/`justify-start` que ya existía ubica el contenido en el lugar correcto sin más ajuste (no hizo falta recalcular paddings desde cero, alcanzó con la proporción del contenedor). No hay `bg-mobile` aparte — mobile reusa la misma foto de fondo, solo cambia el recorte vía `object-cover` sobre el contenedor más angosto/alto.
- **Título del bloque 1 centrado en los dos breakpoints** (`headingClassName: "text-center md:text-center"`): los mockups (desktop original y mobile) lo muestran alineado a la izquierda, pero se dejó centrado a pedido del cliente — así había quedado ya el desktop en una vuelta anterior (2026-08-07), y se unificó mobile al mismo criterio en vez de mantener la alineación del mockup.
- **`servicios-1-bg.webp` (fondo del bloque 1) reemplazado por `servicios-1-bg-alt.webp`**: mismo predio/toma, pero con un fade a negro real (no solo el grading tenue del original) cubriendo aprox. el tercio inferior de la foto — da más contraste al bloque de texto, que ahora es más alto por el cambio de `aspect-ratio` a retrato en mobile. Fuente: `designs/servicios/servicios-bg-1-alt.webp` (nombre con guiones en distinto orden que el resto, ojo si se busca por convención de nombres). El archivo `servicios-1-bg.webp` (sin fade fuerte) queda sin uso — no se borró por las dudas, pero ningún bloque lo referencia ya.
- **⚠️ Inconsistencia de copy sin resolver**: el mockup mobile del bloque 1 dice "Alquiler y **venta** de naves industriales." (singular), el desktop dice "Alquiler y **ventas** de..." (plural, con S) — se replicó cada texto tal cual su propio asset (`heading` para desktop, `headingMobile` para mobile, mismo patrón de líneas manuales que el resto de los títulos), pero es casi seguro que debería ser el mismo texto en los dos breakpoints. Confirmar con el cliente cuál es la versión correcta y unificar (sacar `headingMobile` de ese bloque si terminan siendo iguales).

### 3. Mapa isométrico — zona nueva "Depósito Fiscal, Puerto Seco y Centro de Transferencia de Cargas"

Agregada 2026-08-10 a `app/data/isometrico.ts` (id `deposito-fiscal`), séptima zona sobre el mapa isométrico (`app/components/IsometricMap.tsx`, sin cambios de código — el componente ya es data-driven, solo hacía falta la entrada nueva en `ZONAS_ISOMETRICAS`).

- **Polígono**: el cliente agregó el trazado directo en `designs/isometrico/plano.svg`, pero **suelto al final del archivo, fuera del `<g id="Layer_2">` donde viven los otros 6** — hay que tenerlo presente si se vuelve a tocar ese SVG (no alcanza con iterar el grupo). Coordenadas copiadas tal cual (mismo viewBox 1920×1080, sin offset/escala, igual que el resto del dataset), sacando el punto final duplicado que cierra el polígono en el SVG (el dataset no repite el primer punto al final, a diferencia de cómo lo exporta el SVG).
- **Imagen del bloque iluminado ya estaba puesta** en `public/images/isometrico/deposito-fiscal.webp` (mismo patrón que las otras 6: overlay transparente, solo el bloque a color) — no hizo falta generarla ni pedirla, ya estaba cuando se revisó.
- **Rubro confirmado** (2026-08-10): "Instalaciones del Parque", mismo nombre/color (`#3b82f6`, azul) que esa entrada en `app/data/lotes.ts` (`ZONAS`) — aplicado acá y también retroactivamente a la zona `ingreso`, que comparte el mismo rubro (antes tenía `pillLabel`/`pillColor` en `null`, igual que esta zona nueva antes de confirmarse).
- **⚠️ Bajada todavía pendiente del cliente**: `description` sigue en `null` (solo hay título + rubro confirmados). `ZoneDetailCard.tsx` ya soporta ese caso sin cambios (oculta la bajada si es `null`). `tabLabel` ("Depósito Fiscal") es una etiqueta corta inventada acá para el campo requerido por el tipo — no viene de ningún mockup, y hoy ese campo no se usa en ningún lado del UI (`tabLabel` no tiene ningún consumidor actual, ver `grep`), así que no hay urgencia en confirmarlo.

### 4. Traer formulario/GTM de `sitio-provisorio` y `main`, ocultar VideoZoom

Pedido 2026-08-10: "traer los cambios del formulario, GTM que están en sitio provisorio y main". Antes de portar nada se comparó `develop` contra las dos ramas archivo por archivo — conclusión: **`main` no tenía nada para aportar** (está más atrás que `develop` en todo esto: sin GTM, sin campos obligatorios, sin favicon, sin Resend — probablemente el pedido se refería solo a lo que ya había circulado por `sitio-provisorio`, que en algún momento tomó cosas de `main` también). El GTM (script en `layout.tsx` + evento `form_submit_success` en `Footer.tsx`) y los campos obligatorios (teléfono/mensaje `required`) **ya estaban en `develop`** (commit `aec395e`, previo a esta sesión) y son byte-a-byte idénticos a `sitio-provisorio` — no hizo falta tocarlos.

Lo único que sí faltaba, comparado `develop` vs `sitio-provisorio`:

- **`app/api/contact/route.ts` — envío real de email vía Resend**: antes solo hacía `console.log` del lead (con un TODO). Se trajo el archivo completo de `sitio-provisorio` (tenía exactamente la misma validación de campos que ya estaba en `develop`, más el bloque de Resend). Lee `RESEND_API_KEY`, `RESEND_FROM_EMAIL` (default `PARTA <onboarding@resend.dev>`) y `CONTACT_EMAIL` (default `Info@parta.com.ar`) — sin `RESEND_API_KEY` configurada responde 500 en vez de fallar en silencio.
- **Dependencia `resend` agregada** a `package.json`/`package-lock.json` (`npm install`, sin tocar el lockfile a mano).
- **`.env.example`**: se sumaron las 3 variables de Resend **sin pisar** la de `SHEET_JSON_URL` que ya estaba (el `.env.example` de `sitio-provisorio` no tiene `SHEET_JSON_URL` porque esa rama nunca tuvo el mapa cenital — si se copiaba el archivo entero se perdía esa variable).
- **Favicon real**: `app/favicon.ico` (genérico) reemplazado por `app/icon.png` (logo real de PARTA 512×512, convención de Next App Router — no hace falta referenciarlo en `layout.tsx`, se detecta solo).
- **`.env.local` (no versionado) ya tenía las credenciales de Resend cargadas** desde antes (`RESEND_API_KEY`, `CONTACT_EMAIL=german@aggility.io` como destinatario de prueba en vez del real — revisar antes de producción) — no se tocó.
- **Deliberadamente NO se tocaron** `app/lib/sheet.ts`, los íconos de `Servicios`/`DescripcionGeneral` ni ningún componente de mapas: el diff `develop` vs `sitio-provisorio` los marca como "solo en develop" porque esa rama nunca tuvo esas features (es un sitio mínimo Hero+Footer), no porque haya que borrarlos.

**Video oculto** (mismo pedido, "ocultar el video"): `VideoZoom` sacado del render de `app/page.tsx` (import comentado + `<VideoZoom />` reemplazado por un comentario) pero el componente **no se borró** — sigue en `app/components/VideoZoom.tsx` intacto, listo para reactivar descomentando las 2 líneas si se vuelve a pedir.

**GTM deshabilitado en local** (pedido aparte, 2026-08-10: "ya está instalado Google Analytics y no quiero ensuciar los datos"): `app/layout.tsx` ahora solo monta el script de GTM (y su `<noscript>` de fallback) cuando `process.env.NODE_ENV === "production"` (`GTM_ENABLED`). `next dev` siempre corre con `NODE_ENV=development`, y cualquier deploy real (`next build && next start`, que es lo que corre el hosting) con `production` — no hizo falta ninguna variable de entorno nueva ni tocar `.env.local`. Verificado con `next build`: el HTML generado sigue incluyendo `GTM-5FRR4PVZ`. El `window.dataLayer?.push(...)` de `Footer.tsx` (evento `form_submit_success`) no necesitó cambios: con el script de GTM sin montar, `window.dataLayer` nunca se crea y el optional chaining ya existente lo vuelve un no-op silencioso en local.

### 5. Navbar rediseñado

`app/components/Navbar.tsx` reescrito 2026-08-10 a partir de `designs/navbar/navbar-{desktop,mobile}.png`. Cambia el modelo anterior (logo + botón "Contacto" con chamfer, igual en los dos breakpoints) por uno bien distinto por breakpoint:

- **Desktop**: logo + fila de 6 links de texto plano (sin chamfer/botón): Quiénes somos, Ubicación, Zonificación, Infraestructura y Servicios, Inversores, Contacto.
- **Mobile**: hamburguesa (izq.) + logo centrado + Instagram/LinkedIn (der.) — mismos ícono SVG y URL reales que ya usaba `Footer.tsx` (`instagram.com/parta.arg`, `linkedin.com/company/parta-arg`), no inventados.
- **Mapeo de links a secciones** (confirmado con el cliente): "Quiénes somos" → `#quienes-somos` (nueva id en `DescripcionGeneral.tsx`), "Zonificación" → `#zonificacion` (nueva id en `IsometricMap.tsx`), "Infraestructura y Servicios" → `#servicios` (nueva id en `Servicios.tsx`), "Contacto" → `#contacto` (ya existía, `Footer.tsx`). Las secciones nuevas llevan `scroll-mt-20 md:scroll-mt-24` para que el header fijo no tape el título al llegar por anchor — valor estimado a ojo (altura aprox. del header), no medido pixel-perfect, revisar si queda corto/largo en pantalla real. (El offset real que efectivamente se usa al navegar es el que calcula `Navbar.tsx` en runtime vía `getBoundingClientRect()` — ver nota de Lenis más abajo; el `scroll-mt-*` queda como fallback.)
- **Link "Lotes" agregado** (2026-08-10, no estaba en el mockup original de 6 links, pedido aparte): apunta a `#lotes`, id agregada en el **wrapper** de `CenitalMap.tsx` (no en las `<section>` internas de `CenitalMapDesktop`/`CenitalMapMobile`, que están una oculta con `hidden md:block` y otra con `md:hidden` — poner el id ahí hubiera duplicado el id en el DOM y `querySelector` siempre habría devuelto la variante desktop, con bounding box cero en mobile por estar en `display:none`). Ubicado en el array `NAV_LINKS` entre "Ubicación" y "Zonificación", seguido el mismo criterio que los demás links de reflejar el orden real de las secciones en `page.tsx` (`CenitalMap` va antes que `IsometricMap`).
- **"Ubicación" no tiene sección propia todavía**: primero se mostraba deshabilitado (`<span aria-disabled>`, texto atenuado); el cliente después prefirió sacarlo directamente de la vista (línea comentada en `NAV_LINKS`, no borrada) en vez de mostrarlo apagado. Reactivar es descomentar esa línea + poner el `href` real. "Inversores" ya tiene sección propia (`Inversiones.tsx`, ver más abajo), dejó de estar en esta situación.
- **⚠️ Estado abierto del menú mobile no tiene mockup**: el mockup solo muestra el ícono de hamburguesa cerrado, no cómo se ve el menú desplegado. Se armó una pantalla completa (`bg-[var(--parta-blue)]`, links grandes centrados + Instagram/LinkedIn abajo) siguiendo el lenguaje visual ya establecido del sitio, pero es una interpretación propia — revisar con el cliente antes de darlo por definitivo.
- **✅ Resuelto — sección de destino quedaba pegada arriba (perdía el offset del header) al navegar desde cualquier link del navbar** (reportado 2026-08-10, dos vueltas): el primer intento (restaurar `document.body.style.overflow` síncrono antes del salto nativo) resultó ser un fix parcial/insuficiente — el síntoma empeoró ("ahora no tiene padding top, está todo pegado al top"). **Causa real**: el sitio envuelve todo en `<SmoothScroll>` (`app/components/SmoothScroll.tsx`), que monta una instancia de **Lenis** (scroll suave) con `anchors: false` (default, sin pasar esa opción) — Lenis no intercepta clicks en `<a href="#ancla">`, pero sí sigue leyendo/animando el scroll del documento en su propio loop (`gsap.ticker` + `lenis.raf`). Un salto nativo instantáneo del navegador (que si respeta `scroll-margin-top`/`scroll-mt-*`) termina compitiendo con ese loop en vez de coexistir limpio, y el resultado observado es que el offset del `scroll-mt-*` se pierde. **Fix definitivo**: en vez de depender de la navegación nativa de `#ancla`, `Navbar.tsx` ahora hace `e.preventDefault()` en cada link con href y scrollea a mano vía la propia instancia de Lenis — `scrollToElement()`, exportada desde `SmoothScroll.tsx` (que ahora guarda la instancia en una variable de módulo `lenisInstance`). Las clases `scroll-mt-20 md:scroll-mt-24` de las secciones se dejaron puestas.
- **✅ Resuelto — quedaban ~200px de más entre el header y la sección** (reportado 2026-08-10, tercera vuelta sobre el mismo problema): el primer intento de este fix pasaba además `offset = -alturaRealDelHeader` (medida con `getBoundingClientRect()`) a `scrollToElement`, asumiendo que había que compensar el header a mano. Error: **Lenis ya lee `scroll-margin-top` del elemento destino por su cuenta** (`lenis.mjs`, dentro de `scrollTo()`: `target = rect.top + animatedScroll - scrollMargin - scrollPadding`, exactamente el mismo mecanismo que usa la navegación nativa del navegador) — el offset del header y el `scroll-mt-*` de la sección se estaban restando **los dos**, así que el hueco resultante era la suma de ambos (~80-96px del `scroll-mt-*` + ~70-90px del header medido ≈ los ~200px reportados). Fix: sacar el cálculo manual de altura del header, `scrollToElement(target)` sin offset — el único offset real ahora es el `scroll-mt-20 md:scroll-mt-24` de cada sección (ya no es "solo fallback", es la fuente de verdad única, y Lenis la respeta igual que la navegación nativa).
- **✅ Resuelto — "Contacto" (el único link que nunca tuvo el bug) se rompió con el fix de arriba**: `#contacto` vive en `Footer.tsx`, que **nunca tuvo `scroll-mt-*`** — no hacía falta antes: con el offset manual del header (ver punto anterior) alcanzaba, y al ser la última sección de la página el recorte natural del scroll (no hay más contenido debajo para scrollear) disimulaba cualquier offset de más. Al sacar el offset manual y depender 100% del `scroll-mt-*` de cada sección, Contacto pasó a ser el único sin ninguno — quedó pegado arriba, tapado por el header. Fix: mismo `scroll-mt-20 md:scroll-mt-24` agregado directo en el `<footer id="contacto">`, mismo criterio que las demás secciones. De paso mejora también el otro lugar del sitio que linkea a `#contacto` con navegación nativa (`LoteDetailCard.tsx`, botón "Consultar") — la navegación nativa también respeta `scroll-margin-top`, así que se beneficia del mismo cambio sin tocarlo. **Alcance de este fix**: solo se tocaron los links del propio `Navbar.tsx` (lo reportado). Otros anchors del sitio que usan navegación nativa (ej. "Consultar" → `#contacto` en `LoteDetailCard.tsx`) no se tocaron — no estaban reportados como rotos (probablemente porque `#contacto` es la última sección, un offset de más/menos ahí se nota mucho menos) — si en algún momento se reporta el mismo síntoma ahí, aplicar el mismo patrón (`scrollToElement` en vez de `href` nativo).
- Botón "Contacto" con chamfer que existía en el header (`ChamferOutline`, `chamferClipPath("nav", 7)`) **se sacó** — en el mockup nuevo "Contacto" es un link de texto más, no un botón. `ChamferOutline`/`chamfer.ts` no se tocaron (los sigue usando el resto del sitio).

### 6. Inversores — IMPLEMENTADO

`app/components/Inversiones.tsx`, integrado en `page.tsx` **entre `Servicios` y `Footer`** (misma lógica que el resto: el orden de `page.tsx` refleja el orden de los links del navbar — acá "Inversores" va justo antes de "Contacto"). Activa el link "Inversores" del navbar (`href: "#inversores"`, antes deshabilitado por no tener sección). Assets fuente en `designs/inversiones/`: `inversiones-{1,2,3}-muestra-{desktop,mobile}.png` (mockup completo) y `inversiones-{1,3}-bg-{desktop,mobile}.png` (fondos limpios, convertidos a webp en `public/images/inversiones-{1,3}-bg-{desktop,mobile}.webp` — el bloque 2 no tiene foto, es contenido plano sobre negro).

- **A diferencia de `Servicios.tsx`, acá el cliente pasó fotos DISTINTAS por breakpoint** (no una sola imagen recortada con `object-cover` sobre dos `aspect-ratio` distintos) — se armó como `Hero.tsx` (dos `<Image>`, una `md:hidden` y otra `hidden md:block`), no como Servicios.
- **Todo el bloque 2 va alineado a la izquierda** (título, las 2 bajadas, "¿Cómo funciona?", grilla de pasos, título de cierre) — a diferencia de `DescripcionGeneral`/`Servicios`, que centran. Confirmado mirando el mockup con cuidado (títulos y párrafos con margen izquierdo parejo y borde derecho irregular, no centrados). Los bloques 1 y 3 (fotos) si van centrados en desktop... salvo el bloque 3, que va alineado a la izquierda en desktop pero **centrado en mobile** (`text-center md:text-left` en su `<h2>`) — confirmado por mockup, no asumido: el mockup desktop de ese bloque tiene el título a la izquierda, el mobile lo tiene centrado.
- **Tarjetas de los pasos reusan `chamferClipPath("nav", 16)`** de `chamfer.ts` (mismo componente que `LoteDetailCard`/`ZoneDetailCard`) — confirmado a mano en el mockup que el corte de esquinas es igual (arriba-izq, arriba-der, abajo-der cortadas, abajo-izq recta), no hizo falta inventar una variante nueva para esto.
- **✅ Resuelto — grilla de "¿Cómo funciona?" con el último renglón centrado** (pedido 2026-08-10, tras ajustar el resto a mano): con `grid-cols-3` normal, las tarjetas 04 y 05 (5 ítems en 3 columnas) quedaban en el último renglón arrancando pegadas a la izquierda (columna 3 vacía) — se quería esas 2 centradas a la página, no a la izquierda. Se reemplazó `grid` por `flex flex-wrap justify-center gap-6`, con el ancho de cada tarjeta calculado a mano para replicar 3 columnas (`md:w-[calc((100%-3rem)/3)]`, 3rem = 2 gaps de `gap-6`): con flex-wrap, cada renglón se centra por separado, así el renglón de 3 (que ya ocupa el 100%) no cambia visualmente y el renglón de 2 queda centrado. Mismo patrón a reusar si se agrega/saca alguna tarjeta a futuro (no hardcodear posiciones).
- **Botón "Más información" (bloque 1) terminó usando el chamfer estándar del sitio** (`chamferClipPath("nav", 8)`, mismo `--parta-blue`... revisar, el cliente ajustó el color/forma a mano en una vuelta posterior a la primera implementación — la primera versión probaba una forma de 4 esquinas cortadas con un azul distinto (`#3555fa`, como en Servicios), pero quedó descartada. Si se vuelve a pedir ese estilo puntual, no hay helper ya armado, hay que rehacerlo (`pillChamferClipPath` se sacó del archivo por quedar sin uso).
- **Tamaño y peso del número grande en las tarjetas y de "¿Cómo funciona?"**: ajustados a mano por el cliente en vueltas posteriores (`text-6xl md:text-7xl font-bold` en los números — el peso pasó de `font-normal` a `font-bold` a pedido explícito, "más ancho"; `md:text-7xl` en el subtítulo) — ya no son la aproximación inicial, quedaron confirmados en pantalla real.
- **Nota de copy**: el título del bloque 3 dice "Sé parte del futuro de la industria y la logística." (con punto final, agregado siguiendo el mockup mobile que sí lo tiene, el crop desktop no dejaba verlo con claridad).

### 7. Rentabilidad — IMPLEMENTADO

`app/components/Rentabilidad.tsx`, integrado en `page.tsx` **entre `Inversiones` y `Footer`** (posición pedida explícitamente por el cliente, 2026-08-11 — no inferida). **No estaba en el rediseño original del navbar ni en ningún diseño previo**: primero llegó como una sola página de brochure (formato vertical, sin asset de desktop) como referencia provisoria; después el cliente pasó el **diseño definitivo completo** (`designs/rentabilidad/rentabilidad-{bg,muestra}-{desktop,mobile}.webp`), con fondo propio por breakpoint — se reescribió el componente para usar ese diseño final, mismo patrón que `Servicios`/`Inversiones` (dos `<Image>`, `hidden`/`md:hidden`, no un solo asset recortado por CSS). Los intermediarios del brochure (`rentabilidad-muestra-brochure.png`, `rentabilidad-bg@2x.png`, el recorte manual `rentabilidad-bg.webp` de 1080×1250) quedaron en `designs/rentabilidad/` como historial, ya no son la fuente vigente.

- **No tiene link en el navbar** (a diferencia de todas las demás secciones) — el cliente no lo pidió, solo pidió armar la sección. Sí tiene `id="rentabilidad"` + `scroll-mt-*` por consistencia con el resto. Si se pide sumarla al navbar, es una línea en `NAV_LINKS` de `Navbar.tsx` (mismo patrón que las demás).
- **A diferencia de los bloques-foto de Servicios/Inversiones (contenido anclado abajo), acá el título/bajada/píldoras van arriba**, sobre el cielo de la foto (`justify-start pt-28 md:pt-40` en vez de `justify-end`) — confirmado en el mockup final, no asumido.
- **Alineación mixta, distinta por elemento y por breakpoint** (confirmado mirando los 2 mockups con cuidado, no es un patrón uniforme): título centrado en mobile pero alineado a la izquierda en desktop (`text-center md:text-left`, mismo caso que el bloque 1 de `Servicios.tsx`); bajada **centrada en los dos breakpoints**; píldoras de beneficios siempre alineadas a la izquierda (arrancan del mismo margen que el título en desktop, `flex flex-wrap` sin `justify-center`).
- **Píldoras terminaron usando el chamfer estándar del sitio** (`chamferClipPath("nav", 12)`, mismo azul `#3555fa` que Servicios/Inversiones) — el mockup del brochure anterior las mostraba `rounded-2xl` con otro azul (`#4a60ac`); el diseño definitivo las alinea con el lenguaje visual ya establecido en el resto del sitio, no hubo que inventar nada nuevo esta vez.
- **Copy sin cambios respecto a la referencia del brochure**: título "Una decisión que impacta en la rentabilidad." (3 líneas manuales), bajada "La radicación dentro de PARTA® permite acceder a beneficios que contribuyen directamente a mejorar la competitividad empresarial.", 5 píldoras (Exención de ingresos brutos / Beneficios municipales / Menores costos operativos / Infraestructura compartida / Incentivos vinculados a la radicación industrial).

## Próximos componentes a analizar

(se agregan acá a medida que los revisemos — todavía no iniciado ninguno)
